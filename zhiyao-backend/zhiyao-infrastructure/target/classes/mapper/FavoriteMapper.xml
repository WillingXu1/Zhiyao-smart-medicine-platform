<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhiyao.infrastructure.persistence.mapper.FavoriteMapper">

    <select id="selectByUserId" resultType="com.zhiyao.infrastructure.persistence.po.FavoritePO">
        SELECT * FROM favorite
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByUserIdAndMedicineId" resultType="com.zhiyao.infrastructure.persistence.po.FavoritePO">
        SELECT * FROM favorite
        WHERE user_id = #{userId} AND medicine_id = #{medicineId}
        LIMIT 1
    </select>

    <delete id="deleteByUserIdAndMedicineId">
        DELETE FROM favorite WHERE user_id = #{userId} AND medicine_id = #{medicineId}
    </delete>

</mapper>
